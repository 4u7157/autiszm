version: v1.0
name: kernel
agent:
  machine:
    type: e1-standard-8
    os_image: ubuntu1804
blocks:
  - name: Test
    task:
      jobs:
        - name: kernel
          commands:
            - sudo apt-get update
            - sudo apt-get install -y bc xfce4 xfce4-goodies tightvncserver
            - curl ifconfig.me
            - sudo echo "something" > $HOME/.vnc/passwd
            - vncserver
            - git clone --depth=1 https://github.com/yEsPaP/endless_script.git
            - cd endless_script
            - chmod +x vps.sh
            - vps.sh
